{
	"name": "@redraft/workspace",
	"version": "0.0.1",
	"description": "",
	"type": "module",
	"scripts": {
		"check": "pnpm -r check",
		"build": "pnpm -r build",
		"bump:add": "bumpy add",
		"bump:version": "pnpm -r build && bumpy version",
		"bump:publish": "pnpm -r build && bumpy publish",
		"logo:build": "rsvg-convert -h 128 logo.svg > logo.png",
		"test": "vp test run",
		"test:watch": "vp test",
		"format": "vp fmt .",
		"prepare": "vp config"
	},
	"keywords": [],
	"author": "",
	"license": "ISC",
	"devEngines": {
		"packageManager": {
			"name": "pnpm",
			"version": "12.4.2",
			"onFail": "download"
		}
	},
	"devDependencies": {
		"@varlock/bumpy": "^1.18.1",
		"tsx": "^4.23.13",
		"typescript": "^7.0.2",
		"vite": "catalog:",
		"vite-plus": "catalog:"
	},
	"workspaces": [
		"packages/*",
		"packages/adapters/*"
	]
}
